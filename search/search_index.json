{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"fastapi-crud-generator","text":"<p>Adds full CRUD routes to FastAPI without writing schemas, filters, or pagination by hand. Everything is generated automatically from your ORM model.</p>"},{"location":"#three-lines-to-a-working-api","title":"Three lines to a working API","text":"<pre><code>from fastapi_crud_generator import CRUDCollection\nfrom fastapi_crud_generator.orm.sqlmodel import SQLModelAdapter\n\ncrud = CRUDCollection(orm_adapter=SQLModelAdapter(model=Article, get_session=get_session))\napp.include_router(crud.get_router(prefix=\"/articles\", tags=[\"articles\"]))\n</code></pre> <p>That's enough for a complete set of endpoints:</p> Method URL Description <code>GET</code> <code>/articles</code> List with filtering, sorting, and pagination <code>GET</code> <code>/articles/{article_id}</code> Get by ID <code>POST</code> <code>/articles</code> Create <code>PATCH</code> <code>/articles/{article_id}</code> Partial update <code>DELETE</code> <code>/articles/{article_id}</code> Delete <p>Request/response schemas, filters, sorting, and pagination are all derived from the ORM model \u2014 no extra code needed.</p>"},{"location":"#installation","title":"Installation","text":"<p>If SQLModel, SQLAlchemy, or Tortoise is already in your project:</p> <pre><code>pip install fastapi-crud-generator\n</code></pre> <p>To install an ORM together with the package:</p> <pre><code>pip install \"fastapi-crud-generator[sqlmodel]\"\npip install \"fastapi-crud-generator[sqlalchemy]\"\npip install \"fastapi-crud-generator[tortoise]\"\n</code></pre>"},{"location":"#what-you-get-out-of-the-box","title":"What you get out of the box","text":"<ul> <li>Auto schemas \u2014 public, create, and update schemas generated from the ORM model; no <code>ArticleCreate(BaseModel)</code> boilerplate</li> <li>Filtering \u2014 <code>?title=hello&amp;published=true</code> with no extra code</li> <li>Sorting \u2014 <code>?sort=created_at:desc</code></li> <li>Pagination \u2014 <code>?page=2&amp;per_page=10</code>, response includes <code>count</code>, <code>page</code>, <code>data</code></li> <li>Nested resources \u2014 <code>/threads/{thread_id}/posts/{post_id}</code> via <code>add_nested_collection</code></li> <li>SQLModel, SQLAlchemy, Tortoise ORM support</li> <li>Customizable \u2014 replace any schema with your own, override any handler</li> </ul>"},{"location":"#next-steps","title":"Next steps","text":"<p>Getting Started \u2014 full working example from scratch.</p>"},{"location":"api-reference/","title":"API Reference","text":""},{"location":"api-reference/#crudcollection","title":"CRUDCollection","text":""},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection","title":"<code>CRUDCollection</code>","text":"<p>               Bases: <code>CRUDCollectionBase</code></p> <p>Concrete CRUD collection \u2014 subclass and set orm_adapter to use.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.extra","title":"<code>extra</code>  <code>property</code>","text":"<p>User-defined routes merged into <code>get_router</code> (see ExtraRoutes).</p> <p>Lazily created so the class attribute footprint stays empty until a route is actually registered on an instance.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.__init__","title":"<code>__init__(orm_adapter=None, public_schema=None, public_list_schema=None, update_schema=None, update_out_schema=None, create_schema=None, create_out_schema=None, base_fields=None, public_fields=None, create_fields=None, update_fields=None, filter_schema=None, sort_schema=None, include_schema=None, pk_fields=None, dependencies=None, get_one_dependencies=None, get_many_dependencies=None, create_dependencies=None, update_dependencies=None, delete_dependencies=None, disable_get_one=None, disable_get_many=None, disable_create=None, disable_update=None, disable_delete=None, dependency_overrides=None, **router_kwargs)</code>","text":"<p>Initialise the collection, resolving schemas and dependencies.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.apply_pk_aliases","title":"<code>apply_pk_aliases(pk_schema)</code>","text":"<p>Return pk_schema with model-name-prefixed aliases on plain fields.</p> <p>Fields whose names contain no underscore get an alias <code>{model_name}_{field}</code> (e.g. <code>id</code> \u2192 <code>user_id</code>). Override to customise aliasing behaviour.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.verify_orm_adapter","title":"<code>verify_orm_adapter()</code>","text":"<p>Raise if orm_adapter is not set.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.get_resolved_overrides","title":"<code>get_resolved_overrides(strategy=None)</code>","text":"<p>Merge default overrides with user-supplied dependency_overrides.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.override_dependencies","title":"<code>override_dependencies(original_func, overrides=None)</code>","text":"<p>Replace marker annotations in a handler's signature with real DI.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.get_one_handler","title":"<code>get_one_handler(pk_field_values, include_data, parent_refs)</code>  <code>async</code>","text":"<p>Return a single object or call get_one_not_found if missing.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.get_one_not_found","title":"<code>get_one_not_found(_pk_field_values, _include_data)</code>  <code>async</code>","text":"<p>Raise 404 by default; override to customise not-found behavior.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.get_many_handler","title":"<code>get_many_handler(paginator, filter_data, sort_data, include_data, parent_refs)</code>  <code>async</code>","text":"<p>Handle GET / \u2014 return a paginated, filtered, sorted list.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.create_one_handler","title":"<code>create_one_handler(create_data, parent_refs)</code>  <code>async</code>","text":"<p>Handle POST / \u2014 persist a new object and return it.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.update_one_handler","title":"<code>update_one_handler(pk_field_values, update_data, parent_refs)</code>  <code>async</code>","text":"<p>Handle PATCH /{pk} \u2014 partially update an existing object.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.delete_one_handler","title":"<code>delete_one_handler(pk_field_values, parent_refs)</code>  <code>async</code>","text":"<p>Handle DELETE /{pk} \u2014 delete an object by primary key.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.generate_unique_id","title":"<code>generate_unique_id(route)</code>","text":"<p>Build a unique OpenAPI operation ID from name, path and method.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.get_id_path","title":"<code>get_id_path(exclude=frozenset())</code>","text":"<p>URL path segment for single-object endpoints, e.g. /{user_id}.</p> <p><code>exclude</code> drops pk params already bound by an ancestor's path, so a nested collection does not repeat them. Override this to pin a fixed segment (e.g. <code>return \"/me\"</code> for a token-scoped singleton, where the identity comes from auth, not the URL).</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.add_get_many_route","title":"<code>add_get_many_route(router, strategy=None)</code>","text":"<p>Register GET / on the router.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.add_get_one_route","title":"<code>add_get_one_route(router, strategy=None)</code>","text":"<p>Register GET /{pk} on the router.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.add_create_one_route","title":"<code>add_create_one_route(router, strategy=None)</code>","text":"<p>Register POST / on the router.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.add_update_one_route","title":"<code>add_update_one_route(router, strategy=None)</code>","text":"<p>Register PATCH /{pk} on the router.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.add_delete_one_route","title":"<code>add_delete_one_route(router, strategy=None)</code>","text":"<p>Register DELETE /{pk} on the router.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.add_extra_routes","title":"<code>add_extra_routes(router, strategy=None)</code>","text":"<p>Mount user-defined routes: item under id_path, collection at root.</p> <p>Both anchors reuse the strategy overrides of the CRUD routes, so pk and nested parent-pk injection happen exactly as for get_one / get_many.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.add_nested_collection","title":"<code>add_nested_collection(path, child, config=None)</code>","text":"<p>Register child as a nested collection under this collection.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollection.get_router","title":"<code>get_router(strategy=None, **router_kwargs)</code>","text":"<p>Build and return an APIRouter with all enabled CRUD routes.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase","title":"<code>CRUDCollectionBase</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Base class for CRUD route collections.</p> <p>Subclass this (or <code>CRUDCollection</code>) to create a set of CRUD endpoints for a given ORM adapter. Override handler methods or class attributes to customise behaviour without touching routing or dependency wiring.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.extra","title":"<code>extra</code>  <code>property</code>","text":"<p>User-defined routes merged into <code>get_router</code> (see ExtraRoutes).</p> <p>Lazily created so the class attribute footprint stays empty until a route is actually registered on an instance.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.__init__","title":"<code>__init__(orm_adapter=None, public_schema=None, public_list_schema=None, update_schema=None, update_out_schema=None, create_schema=None, create_out_schema=None, base_fields=None, public_fields=None, create_fields=None, update_fields=None, filter_schema=None, sort_schema=None, include_schema=None, pk_fields=None, dependencies=None, get_one_dependencies=None, get_many_dependencies=None, create_dependencies=None, update_dependencies=None, delete_dependencies=None, disable_get_one=None, disable_get_many=None, disable_create=None, disable_update=None, disable_delete=None, dependency_overrides=None, **router_kwargs)</code>","text":"<p>Initialise the collection, resolving schemas and dependencies.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.apply_pk_aliases","title":"<code>apply_pk_aliases(pk_schema)</code>","text":"<p>Return pk_schema with model-name-prefixed aliases on plain fields.</p> <p>Fields whose names contain no underscore get an alias <code>{model_name}_{field}</code> (e.g. <code>id</code> \u2192 <code>user_id</code>). Override to customise aliasing behaviour.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.verify_orm_adapter","title":"<code>verify_orm_adapter()</code>","text":"<p>Raise if orm_adapter is not set.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.get_resolved_overrides","title":"<code>get_resolved_overrides(strategy=None)</code>","text":"<p>Merge default overrides with user-supplied dependency_overrides.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.override_dependencies","title":"<code>override_dependencies(original_func, overrides=None)</code>","text":"<p>Replace marker annotations in a handler's signature with real DI.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.get_one_handler","title":"<code>get_one_handler(pk_field_values, include_data, parent_refs)</code>  <code>async</code>","text":"<p>Return a single object or call get_one_not_found if missing.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.get_one_not_found","title":"<code>get_one_not_found(_pk_field_values, _include_data)</code>  <code>async</code>","text":"<p>Raise 404 by default; override to customise not-found behavior.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.get_many_handler","title":"<code>get_many_handler(paginator, filter_data, sort_data, include_data, parent_refs)</code>  <code>async</code>","text":"<p>Handle GET / \u2014 return a paginated, filtered, sorted list.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.create_one_handler","title":"<code>create_one_handler(create_data, parent_refs)</code>  <code>async</code>","text":"<p>Handle POST / \u2014 persist a new object and return it.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.update_one_handler","title":"<code>update_one_handler(pk_field_values, update_data, parent_refs)</code>  <code>async</code>","text":"<p>Handle PATCH /{pk} \u2014 partially update an existing object.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.delete_one_handler","title":"<code>delete_one_handler(pk_field_values, parent_refs)</code>  <code>async</code>","text":"<p>Handle DELETE /{pk} \u2014 delete an object by primary key.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.generate_unique_id","title":"<code>generate_unique_id(route)</code>","text":"<p>Build a unique OpenAPI operation ID from name, path and method.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.get_id_path","title":"<code>get_id_path(exclude=frozenset())</code>","text":"<p>URL path segment for single-object endpoints, e.g. /{user_id}.</p> <p><code>exclude</code> drops pk params already bound by an ancestor's path, so a nested collection does not repeat them. Override this to pin a fixed segment (e.g. <code>return \"/me\"</code> for a token-scoped singleton, where the identity comes from auth, not the URL).</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.add_get_many_route","title":"<code>add_get_many_route(router, strategy=None)</code>","text":"<p>Register GET / on the router.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.add_get_one_route","title":"<code>add_get_one_route(router, strategy=None)</code>","text":"<p>Register GET /{pk} on the router.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.add_create_one_route","title":"<code>add_create_one_route(router, strategy=None)</code>","text":"<p>Register POST / on the router.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.add_update_one_route","title":"<code>add_update_one_route(router, strategy=None)</code>","text":"<p>Register PATCH /{pk} on the router.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.add_delete_one_route","title":"<code>add_delete_one_route(router, strategy=None)</code>","text":"<p>Register DELETE /{pk} on the router.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.add_extra_routes","title":"<code>add_extra_routes(router, strategy=None)</code>","text":"<p>Mount user-defined routes: item under id_path, collection at root.</p> <p>Both anchors reuse the strategy overrides of the CRUD routes, so pk and nested parent-pk injection happen exactly as for get_one / get_many.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.add_nested_collection","title":"<code>add_nested_collection(path, child, config=None)</code>","text":"<p>Register child as a nested collection under this collection.</p>"},{"location":"api-reference/#fastapi_crud_generator.crud_generator.CRUDCollectionBase.get_router","title":"<code>get_router(strategy=None, **router_kwargs)</code>","text":"<p>Build and return an APIRouter with all enabled CRUD routes.</p>"},{"location":"api-reference/#adapters","title":"Adapters","text":""},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter","title":"<code>SQLModelAdapter</code>","text":"<p>               Bases: <code>ORMAdapterBase</code></p> <p>ORM adapter for SQLModel with async SQLAlchemy sessions.</p> <p>Implements schema generation by introspecting SQLModel metadata: primary keys via <code>__table__.primary_key</code>, server-generated fields via <code>server_default</code>, and output-only fields via <code>exclude=True</code>. ORM-specific <code>FieldInfoMetadata</code> is stripped from all generated schemas so the resulting Pydantic models are clean of SQLAlchemy internals.</p> <p>Parameters:</p> Name Type Description Default <code>get_session</code> <code>Callable[[], AsyncGenerator[AsyncSession, None, None]]</code> <p>Async generator that yields an <code>AsyncSession</code>.</p> <code>None</code> <code>model</code> <code>type[SQLModel] | None</code> <p>The SQLModel table class to operate on.</p> <code>None</code>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.get_server_generated_field_names","title":"<code>get_server_generated_field_names()</code>","text":"<p>Return field names backed by a column with server_default.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.get_default_public_fields","title":"<code>get_default_public_fields()</code>","text":"<p>Return all model field names that are not explicitly excluded.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.get_default_create_fields","title":"<code>get_default_create_fields(exclude_related=None)</code>","text":"<p>Return field names except auto-generated PKs and server fields.</p> <p>A PK field is excluded when it has no default (auto-increment/uuid4) or when it is a FK pointing to a model in <code>exclude_related</code> \u2014 meaning its value comes from the parent URL, not the request body.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.generate_flat_public_schema","title":"<code>generate_flat_public_schema(fields=None, base_fields=None)</code>","text":"<p>Build a schema from model fields only, without relationships.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.get_default_update_fields","title":"<code>get_default_update_fields()</code>","text":"<p>Return field names except all PKs and server-generated fields.</p> <p>Unlike create, ALL PK fields are excluded \u2014 they come from the URL path and must never be changed via the request body.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.generate_pk_schema","title":"<code>generate_pk_schema()</code>","text":"<p>Build a Pydantic model containing only the primary key fields.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.generate_include_schema","title":"<code>generate_include_schema()</code>","text":"<p>Build a query schema for selecting which relationships to include.</p> <p>Creates a Pydantic model with an <code>include</code> field typed as <code>Literal[&lt;relationship names&gt;]</code>, so FastAPI validates that only existing relationship names are accepted as query parameters.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.get_base_list_queryset","title":"<code>get_base_list_queryset(parent_refs=None)</code>","text":"<p>Build the base SELECT for list ops, scoped to parent.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.get_base_single_queryset","title":"<code>get_base_single_queryset(parent_refs=None)</code>","text":"<p>Build the base SELECT for single-record ops, scoped to parent.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.get_one","title":"<code>get_one(pk_values, include_data, parent_refs=None)</code>  <code>async</code>","text":"<p>Return a single object by primary key, or None if not found.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.get_many","title":"<code>get_many(filter_data, sort_data, include_data, paginator, parent_refs=None)</code>  <code>async</code>","text":"<p>Return a paginated, filtered, sorted list.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.create_one","title":"<code>create_one(data, parent_refs=None)</code>  <code>async</code>","text":"<p>Persist a new object, verifying the parent chain first.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.update_one","title":"<code>update_one(pk_values, data, parent_refs=None)</code>  <code>async</code>","text":"<p>Update an existing object by primary key, scoped to parent chain.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlmodel.SQLModelAdapter.delete_one","title":"<code>delete_one(pk_values, parent_refs=None)</code>  <code>async</code>","text":"<p>Delete an object by primary key, scoped to parent chain.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.sqlalchemy.SQLAlchemyAdapter","title":"<code>SQLAlchemyAdapter</code>","text":"<p>               Bases: <code>ORMAdapterBase</code></p> <p>ORM adapter for SQLAlchemy async (DeclarativeBase) models.</p> <p>Works with any model that inherits from SQLAlchemy's <code>DeclarativeBase</code>. Schemas are generated by introspecting the SA mapper \u2014 no Pydantic annotations required on the model.</p> <p>Parameters:</p> Name Type Description Default <code>get_session</code> <code>Callable[[], AsyncGenerator[AsyncSession, None]]</code> <p>Async generator yielding an <code>AsyncSession</code>.</p> <code>None</code> <code>model</code> <code>type | None</code> <p>The declarative ORM model class to operate on.</p> <code>None</code>"},{"location":"api-reference/#fastapi_crud_generator.orm.tortoise.TortoiseAdapter","title":"<code>TortoiseAdapter</code>","text":"<p>               Bases: <code>ORMAdapterBase</code></p> <p>ORM adapter for Tortoise ORM models.</p> <p>Tortoise uses a global connection pool (<code>Tortoise.init</code>), so unlike SQLAlchemy-based adapters there is no <code>get_session</code> factory. Pass the Tortoise model class directly.</p> <p>Composite primary keys are not supported by Tortoise; this adapter assumes a single <code>pk</code> field per model.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>type | None</code> <p>The Tortoise ORM model class.</p> <code>None</code>"},{"location":"api-reference/#fastapi_crud_generator.orm.base.ORMAdapterBase","title":"<code>ORMAdapterBase</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for ORM adapters.</p> <p>An adapter bridges a specific ORM and <code>CRUDCollection</code> by implementing schema generation and pure data-access methods. Each ORM has its own adapter subclass (e.g. <code>SQLModelAdapter</code>) that translates the generic interface into ORM-specific queries and schema introspection.</p> <p>HTTP concerns (routing, status codes, dependency injection) live in <code>CRUDCollection</code>, not here.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.base.ORMAdapterBase.generate_public_schema","title":"<code>generate_public_schema(fields=None, base_fields=None)</code>  <code>abstractmethod</code>","text":"<p>Build a Pydantic model for API read responses.</p> <p>Fields are resolved in priority order:</p> <ol> <li><code>fields</code> argument (main) + <code>base_fields</code> \u2014 unioned together.</li> <li><code>crud_config.public_fields</code> (main) + <code>crud_config.base_fields</code>    \u2014 used when the corresponding explicit argument is omitted.</li> <li>Adapter default \u2014 all non-excluded fields plus the primary key.    Applied only when both main and base resolve to nothing.    If only <code>base_fields</code> is set, the result is <code>base_fields</code>    alone; the default is skipped.</li> </ol> <p>Parameters:</p> Name Type Description Default <code>fields</code> <code>set[str] | None</code> <p>Main field set. Overrides <code>crud_config.public_fields</code> and adapter defaults.</p> <code>None</code> <code>base_fields</code> <code>set[str] | None</code> <p>Extra fields unioned with the main set. Overrides <code>crud_config.base_fields</code> when provided.</p> <code>None</code> <p>Returns:</p> Type Description <code>type[BaseModel]</code> <p>A Pydantic model class containing only the resolved fields.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.base.ORMAdapterBase.generate_create_schema","title":"<code>generate_create_schema(fields=None, base_fields=None, exclude_related=None)</code>  <code>abstractmethod</code>","text":"<p>Build a Pydantic model for object creation payloads.</p> <p>Fields are resolved in priority order:</p> <ol> <li><code>fields</code> argument (main) + <code>base_fields</code> \u2014 unioned together.</li> <li><code>crud_config.create_fields</code> (main) + <code>crud_config.base_fields</code>    \u2014 used when the corresponding explicit argument is omitted.</li> <li>Adapter default \u2014 all fields except primary keys and fields whose    values are generated by the database (e.g. <code>server_default</code>).    Applied only when both main and base resolve to nothing.    If only <code>base_fields</code> is set, the result is <code>base_fields</code>    alone; the default is skipped.</li> </ol> <p>Note: fields marked <code>exclude=True</code> are not excluded here \u2014 that marker controls serialization of responses, not input acceptance.</p> <p>Parameters:</p> Name Type Description Default <code>fields</code> <code>set[str] | None</code> <p>Main field set. Overrides <code>crud_config.create_fields</code> and adapter defaults.</p> <code>None</code> <code>base_fields</code> <code>set[str] | None</code> <p>Extra fields unioned with the main set. Overrides <code>crud_config.base_fields</code> when provided.</p> <code>None</code> <p>Returns:</p> Type Description <code>type[BaseModel]</code> <p>A Pydantic model class containing only the resolved fields.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.base.ORMAdapterBase.generate_update_schema","title":"<code>generate_update_schema(fields=None, base_fields=None)</code>  <code>abstractmethod</code>","text":"<p>Build a Pydantic model for partial update (PATCH) payloads.</p> <p>Uses the same field resolution logic as <code>generate_create_schema</code>, but every field is made optional so clients only send what they want to change.</p> <p>Parameters:</p> Name Type Description Default <code>fields</code> <code>set[str] | None</code> <p>Main field set. Overrides <code>crud_config.update_fields</code> and adapter defaults.</p> <code>None</code> <code>base_fields</code> <code>set[str] | None</code> <p>Extra fields unioned with the main set. Overrides <code>crud_config.base_fields</code> when provided.</p> <code>None</code> <p>Returns:</p> Type Description <code>type[BaseModel]</code> <p>A Pydantic model class where every field is optional.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.base.ORMAdapterBase.get_model_name","title":"<code>get_model_name()</code>","text":"<p>Return the lowercase model name, or empty string if unavailable.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.base.ORMAdapterBase.generate_pk_schema","title":"<code>generate_pk_schema()</code>  <code>abstractmethod</code>","text":"<p>Build a Pydantic model containing only the primary key fields.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.base.ORMAdapterBase.generate_include_schema","title":"<code>generate_include_schema()</code>  <code>abstractmethod</code>","text":"<p>Build a query schema for selecting which relationships to include.</p> <p>The returned model should have an <code>include</code> field that accepts a list of valid relationship names. FastAPI uses it to validate that only existing relationships are requested as query parameters.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.base.ORMAdapterBase.get_one","title":"<code>get_one(pk_values, include_data, parent_refs=None)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Return a single object by primary key, or None if not found.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.base.ORMAdapterBase.get_many","title":"<code>get_many(filter_data, sort_data, include_data, paginator, parent_refs=None)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Return a paginated, filtered, sorted list.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.base.ORMAdapterBase.create_one","title":"<code>create_one(data, parent_refs=None)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Persist a new object and return it.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.base.ORMAdapterBase.update_one","title":"<code>update_one(pk_values, data, parent_refs=None)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Update an existing object by primary key.</p>"},{"location":"api-reference/#fastapi_crud_generator.orm.base.ORMAdapterBase.delete_one","title":"<code>delete_one(pk_values, parent_refs=None)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Delete an object by primary key and return it, or None.</p>"},{"location":"api-reference/#configuration","title":"Configuration","text":""},{"location":"api-reference/#fastapi_crud_generator.config.CRUDConfigDict","title":"<code>CRUDConfigDict</code>  <code>dataclass</code>","text":"<p>Configuration for automatic CRUD schema generation.</p> <p>Place on a SQLModel table class as <code>crud_config</code> to control how the ORM adapter generates schemas and routes.</p> <p>Attributes:</p> Name Type Description <code>base_fields</code> <code>set[str]</code> <p>Fields shared across schemas; used as a fallback when public_fields, create_fields or update_fields are not set.</p> <code>public_fields</code> <code>set[str]</code> <p>Fields exposed in the public (GET) schema.</p> <code>create_fields</code> <code>set[str]</code> <p>Fields accepted in the create (POST) schema.</p> <code>update_fields</code> <code>set[str]</code> <p>Fields accepted in the update (PATCH) schema.</p>"},{"location":"api-reference/#fastapi_crud_generator.config.NestedConfig","title":"<code>NestedConfig</code>  <code>dataclass</code>","text":"<p>Configuration for a nested collection.</p> <p>Passed as <code>config</code> to <code>add_nested_collection</code>.</p> <p><code>router_kwargs</code> accepts any <code>APIRouter.include_router</code> parameters (tags, dependencies, deprecated, include_in_schema, responses).</p>"},{"location":"api-reference/#pagination","title":"Pagination","text":""},{"location":"api-reference/#fastapi_crud_generator.paginator.PaginatorBase","title":"<code>PaginatorBase</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Base class for paginators injected as FastAPI dependencies.</p> <p>Subclass and implement <code>paginate</code> to provide ORM-specific query execution. <code>page</code> and <code>per_page</code> are read from query parameters automatically.</p>"},{"location":"api-reference/#fastapi_crud_generator.paginator.PaginatorBase.set_page","title":"<code>set_page(page)</code>","text":"<p>Set current page and recalculate limit/offset.</p>"},{"location":"api-reference/#fastapi_crud_generator.paginator.PaginatorBase.set_per_page","title":"<code>set_per_page(per_page)</code>","text":"<p>Set page size and recalculate limit/offset.</p>"},{"location":"api-reference/#fastapi_crud_generator.paginator.PaginatorBase.calc_offset_limit","title":"<code>calc_offset_limit()</code>","text":"<p>Recalculate limit and offset from current page and per_page.</p>"},{"location":"api-reference/#fastapi_crud_generator.paginator.PaginatorBase.paginate","title":"<code>paginate()</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Execute the query and return a paginated result page.</p>"},{"location":"api-reference/#fastapi_crud_generator.schemas.PaginatorPage","title":"<code>PaginatorPage</code>","text":"<p>               Bases: <code>BaseModel</code>, <code>Generic[ListItemT]</code></p>"},{"location":"api-reference/#schemas-errors","title":"Schemas &amp; Errors","text":""},{"location":"api-reference/#fastapi_crud_generator.schemas.ParentRef","title":"<code>ParentRef</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Generic[ModelT]</code></p> <p>Reference to a parent collection item in a nested route.</p> <p>Passed to ORM adapter methods so they can filter, set foreign keys, or verify ownership relative to the parent object.</p> <p>Attributes:</p> Name Type Description <code>model</code> <code>type[ModelT]</code> <p>The parent's ORM model class (e.g. <code>User</code>).</p> <code>pk_values</code> <code>BaseModel</code> <p>The parent's primary key values parsed from the URL.</p>"},{"location":"api-reference/#fastapi_crud_generator.schemas.NotFoundError","title":"<code>NotFoundError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised by ORM adapters when a requested resource does not exist.</p>"},{"location":"api-reference/#fastapi_crud_generator.schemas.ParentNotFoundError","title":"<code>ParentNotFoundError</code>","text":"<p>               Bases: <code>NotFoundError</code></p> <p>Raised when a parent resource does not exist or is not accessible.</p>"},{"location":"custom-adapter/","title":"Custom Adapter","text":"<p>To connect an ORM or data source that has no built-in adapter, implement <code>ORMAdapterBase</code>.</p>"},{"location":"custom-adapter/#interface","title":"Interface","text":"<pre><code>from fastapi_crud_generator.orm.base import ORMAdapterBase\nfrom fastapi_crud_generator.schemas import PaginatorPage, ParentRef\nfrom pydantic import BaseModel\n\nclass MyAdapter(ORMAdapterBase):\n    # Schema generation\n    def generate_public_schema(self, fields=None, base_fields=None) -&gt; type[BaseModel]: ...\n    def generate_create_schema(self, fields=None, base_fields=None, exclude_related=None) -&gt; type[BaseModel]: ...\n    def generate_update_schema(self, fields=None, base_fields=None) -&gt; type[BaseModel]: ...\n    def generate_pk_schema(self) -&gt; type[BaseModel]: ...\n    def generate_include_schema(self) -&gt; type[BaseModel]: ...\n\n    # CRUD operations\n    async def get_one(self, pk_values, include_data, parent_refs=None): ...\n    async def get_many(self, filter_data, sort_data, include_data, paginator, parent_refs=None): ...\n    async def create_one(self, data, parent_refs=None): ...\n    async def update_one(self, pk_values, data, parent_refs=None): ...\n    async def delete_one(self, pk_values, parent_refs=None): ...\n</code></pre>"},{"location":"custom-adapter/#example-in-memory-store","title":"Example: in-memory store","text":"<pre><code>from pydantic import BaseModel, create_model\nfrom fastapi_crud_generator.orm.base import ORMAdapterBase\nfrom fastapi_crud_generator.schemas import NotFoundError\n\n_store: dict[int, dict] = {}\n_next_id = 1\n\nclass ItemPublic(BaseModel):\n    id: int\n    name: str\n    price: float\n\nclass ItemCreate(BaseModel):\n    name: str\n    price: float\n\nclass ItemUpdate(BaseModel):\n    name: str | None = None\n    price: float | None = None\n\nclass ItemPK(BaseModel):\n    id: int\n\nclass InMemoryAdapter(ORMAdapterBase):\n    def generate_public_schema(self, fields=None, base_fields=None):\n        return ItemPublic\n\n    def generate_create_schema(self, fields=None, base_fields=None, exclude_related=None):\n        return ItemCreate\n\n    def generate_update_schema(self, fields=None, base_fields=None):\n        return ItemUpdate\n\n    def generate_pk_schema(self):\n        return ItemPK\n\n    def generate_include_schema(self):\n        return create_model(\"ItemInclude\")\n\n    async def get_one(self, pk_values, include_data, parent_refs=None):\n        return _store.get(pk_values.id)\n\n    async def get_many(self, filter_data, sort_data, include_data, paginator, parent_refs=None):\n        items = list(_store.values())\n        page = items[paginator.offset : paginator.offset + paginator.limit]\n        return {\"page\": paginator.page, \"per_page\": paginator.per_page, \"count\": len(items), \"data\": page}\n\n    async def create_one(self, data, parent_refs=None):\n        global _next_id\n        item = {\"id\": _next_id, **data.model_dump()}\n        _store[_next_id] = item\n        _next_id += 1\n        return item\n\n    async def update_one(self, pk_values, data, parent_refs=None):\n        if pk_values.id not in _store:\n            raise NotFoundError\n        for k, v in data.model_dump(exclude_none=True).items():\n            _store[pk_values.id][k] = v\n\n    async def delete_one(self, pk_values, parent_refs=None):\n        return _store.pop(pk_values.id, None)\n</code></pre> <p>Usage:</p> <pre><code>crud = CRUDCollection(orm_adapter=InMemoryAdapter())\napp.include_router(crud.get_router(prefix=\"/items\"))\n</code></pre>"},{"location":"custom-adapter/#parent_refs-in-nested-resources","title":"<code>parent_refs</code> in nested resources","text":"<p>In nested routes, CRUD methods receive <code>parent_refs</code> \u2014 a list of <code>ParentRef</code> objects describing the parent items in the URL:</p> <pre><code>from fastapi_crud_generator.schemas import ParentRef\n\nasync def get_many(self, filter_data, sort_data, include_data, paginator, parent_refs=None):\n    query = select(Post)\n    for ref in (parent_refs or []):\n        if ref.model is Thread:\n            query = query.where(Post.thread_id == ref.pk_values.thread_id)\n    ...\n</code></pre>"},{"location":"custom-adapter/#full-interface-reference","title":"Full interface reference","text":"<p>See API Reference.</p>"},{"location":"guide/cookbook/","title":"Cookbook","text":"<p>Task-oriented recipes. Every one is exercised by a test in the suite \u2014 the \"Tested by\" line points at it, so the code here is known to run (and runs against every configured ORM backend).</p> <p>Examples assume you already have an adapter:</p> <pre><code>adapter = SQLModelAdapter(model=Category, get_session=get_session)\n</code></pre>"},{"location":"guide/cookbook/#disable-endpoints","title":"Disable endpoints","text":"<p>Drop the routes you don't want to expose.</p> <pre><code>crud = CRUDCollection(\n    orm_adapter=adapter,\n    disable_delete=True,\n    disable_create=True,\n)\n</code></pre> <p>The path stays registered for its remaining methods, so a call to a disabled one returns <code>405</code>, not <code>404</code>.</p> <p>Tested by <code>tests/integration/test_disable.py</code>.</p>"},{"location":"guide/cookbook/#expose-only-some-fields","title":"Expose only some fields","text":"<p>Restrict which model fields feed the generated schemas instead of writing a schema by hand.</p> <pre><code>crud = CRUDCollection(\n    orm_adapter=adapter,\n    public_fields={\"id\", \"title\", \"summary\"},  # what GET returns\n    create_fields={\"title\", \"content\"},         # what POST accepts\n    update_fields={\"title\", \"content\", \"summary\"},\n)\n</code></pre> <p>The same sets can live on the model as a <code>crud_config</code> class variable.</p> <p>Tested by <code>tests/test_sqlmodel_schema_generation.py</code>.</p>"},{"location":"guide/cookbook/#use-a-custom-primary-key-path","title":"Use a custom primary-key path","text":"<p>Supply your own <code>pk_fields</code> model to route on a non-<code>id</code> column.</p> <pre><code>class SlugPK(BaseModel):\n    slug: str\n\ncrud = CRUDCollection(orm_adapter=adapter, pk_fields=SlugPK)\n# -&gt; GET /{slug}\n</code></pre> <p>Tested by <code>tests/test_sqlmodel_crud_collection_pk.py</code>.</p>"},{"location":"guide/cookbook/#nested-collections","title":"Nested collections","text":"<p>Mount one collection under another; the child is automatically scoped to its parent by the foreign key, and the parent id is taken from the URL, not the request body.</p> <pre><code>thread_crud = CRUDCollection(orm_adapter=make_adapter(Thread))\npost_crud = CRUDCollection(orm_adapter=make_adapter(Post))\nthread_crud.add_nested_collection(\"/posts\", post_crud)\n\napp.include_router(thread_crud.get_router(prefix=\"/threads\"))\n# -&gt; /threads/{thread_id}/posts, /threads/{thread_id}/posts/{post_id}\n</code></pre> <p>Nesting works several levels deep, in either FK direction, and with composite keys.</p> <p>Tested by <code>tests/integration/test_nested.py</code>, <code>test_root_posts.py</code>, <code>test_composite.py</code>.</p>"},{"location":"guide/cookbook/#a-me-singleton-collection","title":"A /me singleton collection","text":"<p>Serve <code>/users/me</code> \u2014 where the record is identified by the auth token, not a path id. Combine two overrides: replace the PK dependency with one that reads the current user, and pin the path segment to <code>/me</code>.</p> <pre><code>class UserPK(BaseModel):\n    id: int\n\nasync def get_me() -&gt; UserPK:\n    return UserPK(id=current_user_id())  # from your auth\n\nclass UserMeCRUD(CRUDCollection):\n    dependency_overrides = {\n        PKFieldsDependency: Annotated[UserPK, Depends(get_me)],\n    }\n\n    def get_id_path(self, exclude=frozenset()) -&gt; str:\n        return \"/me\"\n\nuser_me_crud = UserMeCRUD(orm_adapter=make_adapter(User))\nuser_me_crud.add_nested_collection(\"/posts\", CRUDCollection(orm_adapter=make_adapter(Post)))\napp.include_router(user_me_crud.get_router(prefix=\"/users\"))\n# -&gt; /users/me, /users/me/posts  (no {user_id} anywhere)\n</code></pre> <p>Tested by <code>tests/integration/test_user_posts.py</code>.</p>"},{"location":"guide/cookbook/#add-a-custom-route-to-a-collection","title":"Add a custom route to a collection","text":"<p>Use <code>collection.extra</code> to hang your own routes off a generated collection \u2014 no <code>get_router</code> override, no manual <code>add_api_route</code>. Bare verbs anchor on the single object (mounted under <code>get_id_path</code>); <code>.collection</code> anchors on the collection root. Both accept every <code>APIRouter</code> keyword (<code>response_model=</code>, <code>dependencies=</code>, <code>status_code=</code>).</p> <pre><code>user_me_crud = UserMeCRUD(orm_adapter=make_adapter(User))\n\n@user_me_crud.extra.post(\"/avatar\", response_model=UserPublic)\nasync def upload_avatar(\n    file: Annotated[UploadFile, File()],\n) -&gt; User:\n    ...\n# -&gt; POST /users/me/avatar   (item anchor: under get_id_path)\n\n@user_me_crud.extra.collection.get(\"/search\")\nasync def search_users(q: str) -&gt; list[UserPublic]:\n    ...\n# -&gt; GET /users/search       (collection anchor: at the root)\n</code></pre> <p>To receive the object's primary key, declare a parameter with the <code>PKFieldsDependency</code> marker \u2014 it is injected exactly as for <code>get_one</code>, including every ancestor key when the collection is nested, so a handler never spells out parent ids:</p> <pre><code>@post_crud.extra.get(\"/whoami\")\nasync def whoami(pk: Annotated[BaseModel, PKFieldsDependency]) -&gt; dict:\n    return {\"id\": pk.id}\n# nested -&gt; GET /categories/{category_id}/threads/{thread_id}/posts/{post_id}/whoami\n</code></pre> <p><code>extra.item</code> is an explicit alias for the bare form. Collection-root extras are matched before <code>/{pk}</code>, so a literal segment like <code>/search</code> is never swallowed by the get-one route.</p> <p>Tested by <code>tests/integration/test_extra_routes.py</code>.</p>"},{"location":"guide/cookbook/#return-a-fallback-instead-of-404","title":"Return a fallback instead of 404","text":"<p>Override <code>get_one_not_found</code> to return a value when the row is missing. The same hook is how you create-on-first-access.</p> <pre><code>class WithFallback(CRUDCollection):\n    orm_adapter = adapter\n\n    async def get_one_not_found(self, pk_values, include_data):\n        return self.public_schema(id=0, name=\"fallback\")\n# GET a missing id -&gt; 200 with the fallback body\n</code></pre> <p>Tested by <code>tests/integration/test_override_hooks.py</code>.</p>"},{"location":"guide/cookbook/#pre-process-input-in-a-handler","title":"Pre-process input in a handler","text":"<p>Override a route handler to run logic around the adapter call. Keep the marker annotations \u2014 they are what turns the parameters into real request inputs; drop them and the request 422s.</p> <pre><code>class PrefixName(CRUDCollection):\n    orm_adapter = adapter\n\n    async def create_one_handler(\n        self,\n        create_data: Annotated[BaseModel, CreateSchemaDependency],\n        parent_refs: Annotated[list, ParentPKFieldsDependency],\n    ):\n        create_data.name = f\"PFX-{create_data.name}\"\n        return await super().create_one_handler(create_data, parent_refs)\n</code></pre> <p>Tested by <code>tests/integration/test_override_hooks.py</code>.</p>"},{"location":"guide/cookbook/#restrict-which-relations-can-be-included","title":"Restrict which relations can be included","text":"<p>Pass an <code>include_schema</code> that only allows chosen relations; anything else is rejected with <code>422</code>.</p> <pre><code>class PostIncludeThreadOnly(BaseModel):\n    include: list[Literal[\"thread\"]] | None = None\n\ncrud = CRUDCollection(orm_adapter=adapter, include_schema=PostIncludeThreadOnly)\n# ?include=thread -&gt; 200 ; ?include=posts -&gt; 422\n</code></pre> <p>Tested by <code>tests/integration/test_include.py</code>.</p>"},{"location":"guide/cookbook/#load-several-relations-at-once","title":"Load several relations at once","text":"<p>Repeat the <code>include</code> query parameter \u2014 one entry per relation. A comma-joined value is not supported and returns <code>422</code>.</p> <pre><code>GET /threads/{id}?include=category&amp;include=posts   # both loaded\nGET /threads/{id}?include=category,posts           # 422\n</code></pre> <p>Tested by <code>tests/integration/test_include_multi.py</code>.</p>"},{"location":"guide/customization/","title":"Customization","text":"<p>Every collection is configured in one of two interchangeable ways, and its behaviour is tuned by overriding methods on a subclass. This page is the reference for both. For task-oriented, copy-paste examples see the Cookbook \u2014 every recipe there is backed by a test.</p>"},{"location":"guide/customization/#two-ways-to-configure","title":"Two ways to configure","text":"<p>A setting can be passed to the constructor or declared as a class attribute. They are equivalent; the constructor argument wins when both are given.</p> <pre><code># constructor\ncrud = CRUDCollection(orm_adapter=adapter, disable_delete=True)\n\n# class attribute \u2014 handy when several collections share config\nclass ArticleCRUD(CRUDCollection):\n    orm_adapter = adapter\n    disable_delete = True\n</code></pre> <p>Class attributes compose through normal inheritance:</p> <pre><code>class AuthenticatedCRUD(CRUDCollection):\n    dependencies = [Depends(require_auth)]\n\nclass ArticleCRUD(AuthenticatedCRUD):\n    orm_adapter = SQLModelAdapter(model=Article, get_session=get_session)\n</code></pre>"},{"location":"guide/customization/#options-reference","title":"Options reference","text":""},{"location":"guide/customization/#adapter-primary-key","title":"Adapter &amp; primary key","text":"Option Type Default Purpose <code>orm_adapter</code> <code>ORMAdapterBase</code> \u2014 (required) The ORM adapter all data access goes through. <code>pk_fields</code> <code>type[BaseModel]</code> derived from adapter Model of primary-key fields; drives the <code>/{...}</code> path params."},{"location":"guide/customization/#schemas","title":"Schemas","text":"<p>Leave these unset to have them generated from the model; set one to take full control of that shape.</p> Option Type Default <code>public_schema</code> <code>BaseModel</code> <code>adapter.generate_public_schema(...)</code> <code>public_list_schema</code> <code>BaseModel</code> <code>PaginatorPage[public_schema]</code> <code>create_schema</code> <code>BaseModel</code> generated (lazily, see note) <code>update_schema</code> <code>BaseModel</code> generated, all fields optional <code>filter_schema</code> <code>BaseModel</code> one optional query field per scalar <code>sort_schema</code> <code>BaseModel</code> <code>?sort=field:asc\\|desc</code> values <code>include_schema</code> <code>BaseModel</code> one entry per relation <code>create_out_schema</code> / <code>update_out_schema</code> any <code>response_model</code> for POST / PATCH <p><code>create_schema</code> is resolved lazily</p> <p>Unlike the others, the create schema is finalised when the router is built, because a nested collection's strategy may drop the parent's foreign key from it. <code>self.create_schema</code> is therefore <code>None</code> on the instance \u2014 don't construct it directly inside a hook.</p>"},{"location":"guide/customization/#field-selection","title":"Field selection","text":"<p>Instead of writing a whole schema, restrict which model fields feed the generated ones:</p> Option Applies to <code>base_fields</code> fallback for all generated schemas <code>public_fields</code> the public (GET) schema <code>create_fields</code> the create (POST) schema <code>update_fields</code> the update (PATCH) schema <p>The same sets can live on the model itself as a <code>crud_config</code> \u2014 see Schema Generation.</p>"},{"location":"guide/customization/#disabling-endpoints","title":"Disabling endpoints","text":"<p>Each flag drops one route. <code>None</code> (the constructor default) means \"inherit the class attribute\"; <code>True</code>/<code>False</code> set it outright.</p> Option Removes <code>disable_get_many</code> <code>GET /</code> <code>disable_get_one</code> <code>GET /{pk}</code> <code>disable_create</code> <code>POST /</code> <code>disable_update</code> <code>PATCH /{pk}</code> <code>disable_delete</code> <code>DELETE /{pk}</code>"},{"location":"guide/customization/#dependencies","title":"Dependencies","text":"Option Scope <code>dependencies</code> all routes of the collection <code>get_one_dependencies</code> <code>GET /{pk}</code> only <code>get_many_dependencies</code> <code>GET /</code> only <code>create_dependencies</code> <code>POST /</code> only <code>update_dependencies</code> <code>PATCH /{pk}</code> only <code>delete_dependencies</code> <code>DELETE /{pk}</code> only <p>Pass collection-wide deps via <code>dependencies</code>, not <code>router_kwargs</code></p> <p>A <code>dependencies=</code> value smuggled through <code>**router_kwargs</code> is overwritten by <code>self.dependencies</code> when the router is built. Use the <code>dependencies</code> option.</p>"},{"location":"guide/customization/#other","title":"Other","text":"Option Type Purpose <code>dependency_overrides</code> <code>dict[type, ...]</code> Replace a dependency marker (see below). <code>**router_kwargs</code> \u2014 Forwarded to <code>APIRouter(**...)</code> (tags, prefix, etc.)."},{"location":"guide/customization/#overridable-methods","title":"Overridable methods","text":"<p>Subclass <code>CRUDCollection</code> and override what you need. Signatures below are exact \u2014 match them.</p>"},{"location":"guide/customization/#get_id_path","title":"<code>get_id_path</code>","text":"<pre><code>def get_id_path(self, exclude: frozenset[str] = frozenset()) -&gt; str\n</code></pre> <p>Returns the single-object path segment (default <code>/{article_id}</code>, built from <code>pk_fields</code>). <code>exclude</code> drops params an ancestor already placed in the path. Override it to pin a fixed segment for a token-scoped singleton whose identity comes from auth rather than the URL:</p> <pre><code>class UserMeCRUD(CRUDCollection):\n    def get_id_path(self, exclude=frozenset()) -&gt; str:\n        return \"/me\"\n</code></pre> <p>See the <code>/users/me</code> recipe.</p>"},{"location":"guide/customization/#get_one_not_found","title":"<code>get_one_not_found</code>","text":"<pre><code>async def get_one_not_found(self, pk_values, include_data) -&gt; object\n</code></pre> <p>Called by <code>GET /{pk}</code> when the row is missing; the default raises <code>HTTPException(404)</code>. This is the only formal not-found hook \u2014 for create/update/delete the 404 is raised inside the handler itself. Override it to return a fallback, or to create-on-first-access.</p>"},{"location":"guide/customization/#route-handlers","title":"Route handlers","text":"<pre><code>async def get_one_handler(self, pk_field_values, include_data, parent_refs)\nasync def get_many_handler(self, paginator, filter_data, sort_data, include_data, parent_refs)\nasync def create_one_handler(self, create_data, parent_refs)\nasync def update_one_handler(self, pk_field_values, update_data, parent_refs)\nasync def delete_one_handler(self, pk_field_values, parent_refs)\n</code></pre> <p>Override a handler to pre- or post-process around the adapter call.</p> <p>Keep the marker annotations</p> <p>Each handler parameter is annotated <code>Annotated[..., XDependency]</code>. Those markers are what the router rewrites into real request params. An override with plain, unannotated parameters makes them leak into the query string and the request fails with 422. Copy the annotations verbatim \u2014 see the handler recipe.</p>"},{"location":"guide/customization/#apply_pk_aliases-and-generate_unique_id","title":"<code>apply_pk_aliases</code> and <code>generate_unique_id</code>","text":"<pre><code>def apply_pk_aliases(self, pk_schema: type[BaseModel]) -&gt; type[BaseModel]\ndef generate_unique_id(self, route: APIRoute) -&gt; str\n</code></pre> <p><code>apply_pk_aliases</code> adds the <code>{model}_id</code> alias to bare PK fields (so <code>id</code> becomes <code>{article_id}</code> in the path); override to change that naming. <code>generate_unique_id</code> builds the OpenAPI operationId.</p>"},{"location":"guide/customization/#adding-your-own-routes-with-extra","title":"Adding your own routes with <code>extra</code>","text":"<p>When you need an endpoint the generator does not produce (<code>/avatar</code>, <code>/publish</code>, <code>/search</code>), register it on <code>collection.extra</code> instead of overriding <code>get_router</code>. <code>extra</code> is a small registrar exposing the FastAPI verb decorators; the routes are merged into <code>get_router()</code> with the same dependency wiring as the generated routes.</p> <pre><code>collection.extra.get(path, **api_route_kwargs)      # item anchor\ncollection.extra.post(path, ...)                    # (also put/patch/\ncollection.extra.delete(path, ...)                  #  delete/api_route)\ncollection.extra.item                               # alias for the above\ncollection.extra.collection.get(path, ...)          # collection anchor\n</code></pre> Anchor How to register Where it mounts Single object <code>extra.&lt;verb&gt;</code> (or <code>extra.item.&lt;verb&gt;</code>) under <code>get_id_path</code> \u2014 <code>/{pk}/...</code>, or <code>/me/...</code> for a pinned segment Collection root <code>extra.collection.&lt;verb&gt;</code> at the collection root \u2014 <code>/...</code> <p>An empty path targets the anchor itself: <code>extra.patch(\"\")</code> serves <code>/{pk}</code>, <code>extra.collection.post(\"\")</code> serves the collection root. That is how a hand-written handler takes over from a generated one when the body needs processing the generator cannot express \u2014 file uploads, say:</p> <pre><code>collection = ClubCRUD(disable_update=True)   # drop the generated PATCH\n\n@collection.extra.patch(\"\", response_model=Club)\nasync def edit(...): ...\n</code></pre> <p>Disabling the generated route matters: extras are mounted first and win the match, but the generated one would still show up in the schema.</p> <p>Both anchors reuse the strategy overrides of the CRUD routes, so a handler can declare <code>Annotated[BaseModel, PKFieldsDependency]</code> (and <code>Annotated[list, ParentPKFieldsDependency]</code>) to receive the object's key \u2014 and every ancestor key when the collection is nested \u2014 without naming any path param. Collection-root extras are registered before <code>/{pk}</code>, so a literal segment such as <code>/search</code> is not captured by the get-one route.</p> <p>Any <code>APIRouter</code> keyword (<code>response_model=</code>, <code>dependencies=</code>, <code>status_code=</code>, <code>tags=</code>) passes straight through. See the custom-route recipe.</p>"},{"location":"guide/customization/#replacing-a-dependency-with-dependency_overrides","title":"Replacing a dependency with <code>dependency_overrides</code>","text":"<p>Handlers declare their inputs as marker classes from <code>fastapi_crud_generator.deps</code> (e.g. <code>PKFieldsDependency</code>, <code>CreateSchemaDependency</code>). <code>dependency_overrides</code> swaps the implementation behind a marker \u2014 the key is the marker class, the value is either a <code>ReplaceSignatureDependency</code> or a raw annotation (which is wrapped automatically).</p> <p>The canonical use is replacing the primary key with a value from auth, so <code>/{pk}</code> becomes token-scoped:</p> <pre><code>class UserMeCRUD(CRUDCollection):\n    dependency_overrides = {\n        PKFieldsDependency: Annotated[UserPK, Depends(get_current_user)],\n    }\n</code></pre> <p>Markers you can override: <code>CreateSchemaDependency</code>, <code>UpdateSchemaDependency</code>, <code>PublicSchemaDependency</code>, <code>PublicListSchemaDependency</code>, <code>FilterSchemaDependency</code>, <code>SortSchemaDependency</code>, <code>IncludeSchemaDependency</code>, <code>PKFieldsDependency</code>, <code>PaginatorDependency</code>, <code>ParentPKFieldsDependency</code>.</p>"},{"location":"guide/dependencies/","title":"Dependencies","text":"<p>Standard FastAPI dependencies (<code>Depends</code>) can be applied to all routes at once or to a specific operation type.</p>"},{"location":"guide/dependencies/#all-routes","title":"All routes","text":"<pre><code>from fastapi import Depends\nfrom fastapi_crud_generator import CRUDCollection\n\ncrud = CRUDCollection(\n    orm_adapter=adapter,\n    dependencies=[Depends(require_auth)],\n)\n</code></pre>"},{"location":"guide/dependencies/#per-operation-type","title":"Per operation type","text":"<pre><code>crud = CRUDCollection(\n    orm_adapter=adapter,\n    get_one_dependencies=[Depends(require_auth)],\n    get_many_dependencies=[Depends(require_auth)],\n    create_dependencies=[Depends(require_admin)],\n    update_dependencies=[Depends(require_admin)],\n    delete_dependencies=[Depends(require_admin)],\n)\n</code></pre>"},{"location":"guide/dependencies/#example-jwt-authentication","title":"Example: JWT authentication","text":"<pre><code>from fastapi import Depends, HTTPException, Security\nfrom fastapi.security import HTTPBearer, HTTPAuthorizationCredentials\n\nsecurity = HTTPBearer()\n\nasync def require_auth(credentials: HTTPAuthorizationCredentials = Security(security)):\n    if not verify_token(credentials.credentials):\n        raise HTTPException(status_code=401, detail=\"Unauthorized\")\n\ncrud = CRUDCollection(\n    orm_adapter=adapter,\n    dependencies=[Depends(require_auth)],\n)\n</code></pre>"},{"location":"guide/dependencies/#at-router-level","title":"At router level","text":"<p>Dependencies can also be added when mounting the router rather than in <code>CRUDCollection</code>:</p> <pre><code>app.include_router(\n    crud.get_router(prefix=\"/articles\"),\n    dependencies=[Depends(require_auth)],\n)\n</code></pre>"},{"location":"guide/filtering-sorting/","title":"Filtering &amp; Sorting","text":""},{"location":"guide/filtering-sorting/#filtering","title":"Filtering","text":"<p>A filter schema is generated automatically from the public schema. All scalar fields (<code>int</code>, <code>float</code>, <code>str</code>, <code>bool</code>, enum) become optional query parameters.</p> <pre><code>GET /articles?published=true\nGET /articles?title=hello\nGET /articles?published=false&amp;title=draft\n</code></pre> <p>Relation fields (FK objects, lists) are excluded \u2014 only primitive types are supported.</p>"},{"location":"guide/filtering-sorting/#custom-filter-schema","title":"Custom filter schema","text":"<p>If you need different logic (e.g. date range filtering), pass your own schema:</p> <pre><code>from pydantic import BaseModel\nfrom datetime import date\n\nclass ArticleFilter(BaseModel):\n    published: bool | None = None\n    created_after: date | None = None\n    created_before: date | None = None\n\ncrud = CRUDCollection(\n    orm_adapter=adapter,\n    filter_schema=ArticleFilter,\n)\n</code></pre> <p>Note</p> <p>With a custom filter schema, the adapter receives it as-is in <code>get_many</code>. The standard adapters apply only fields that match model column names by exact match. For range filters or custom logic, override the adapter's <code>get_many</code> method.</p>"},{"location":"guide/filtering-sorting/#sorting","title":"Sorting","text":"<p>Sorting is also generated automatically. For each scalar field, three variants are available:</p> <pre><code>GET /articles?sort=title          # ascending by default\nGET /articles?sort=title:asc\nGET /articles?sort=title:desc\n</code></pre> <p>Multiple sort fields are supported:</p> <pre><code>GET /articles?sort=published:desc&amp;sort=title:asc\n</code></pre>"},{"location":"guide/filtering-sorting/#custom-sort-schema","title":"Custom sort schema","text":"<pre><code>from typing import Literal\nfrom pydantic import BaseModel\n\nclass ArticleSort(BaseModel):\n    sort: list[Literal[\"title\", \"title:asc\", \"title:desc\", \"created_at:desc\"]] | None = None\n\ncrud = CRUDCollection(\n    orm_adapter=adapter,\n    sort_schema=ArticleSort,\n)\n</code></pre>"},{"location":"guide/getting-started/","title":"Getting Started","text":"<p>A complete example from scratch using SQLModel. The approach is the same for SQLAlchemy and Tortoise \u2014 see ORM Adapters.</p>"},{"location":"guide/getting-started/#installation","title":"Installation","text":"<pre><code>pip install \"fastapi-crud-generator[sqlmodel]\"\npip install aiosqlite  # or asyncpg for PostgreSQL\n</code></pre>"},{"location":"guide/getting-started/#model","title":"Model","text":"<pre><code>from sqlmodel import Field, SQLModel\n\nclass Article(SQLModel, table=True):\n    id: int | None = Field(default=None, primary_key=True)\n    title: str\n    content: str\n    published: bool = False\n</code></pre> <p>No Pydantic schemas needed \u2014 they are generated automatically.</p>"},{"location":"guide/getting-started/#session","title":"Session","text":"<pre><code>from sqlalchemy.ext.asyncio import create_async_engine\nfrom sqlmodel.ext.asyncio.session import AsyncSession\nfrom collections.abc import AsyncGenerator\n\nengine = create_async_engine(\"sqlite+aiosqlite:///db.sqlite3\")\n\nasync def get_session() -&gt; AsyncGenerator[AsyncSession, None]:\n    async with AsyncSession(engine) as session:\n        yield session\n</code></pre>"},{"location":"guide/getting-started/#wiring-up-crud","title":"Wiring up CRUD","text":"<pre><code>from fastapi import FastAPI\nfrom fastapi_crud_generator import CRUDCollection\nfrom fastapi_crud_generator.orm.sqlmodel import SQLModelAdapter\n\napp = FastAPI()\n\ncrud = CRUDCollection(\n    orm_adapter=SQLModelAdapter(model=Article, get_session=get_session),\n)\napp.include_router(crud.get_router(prefix=\"/articles\", tags=[\"articles\"]))\n</code></pre>"},{"location":"guide/getting-started/#what-you-get","title":"What you get","text":"<p>Five endpoints with full OpenAPI docs at <code>/docs</code>:</p> Method URL Body Response <code>GET</code> <code>/articles</code> \u2014 <code>{ page, per_page, count, data: [...] }</code> <code>GET</code> <code>/articles/{article_id}</code> \u2014 article object <code>POST</code> <code>/articles</code> <code>{ title, content, published }</code> created object <code>PATCH</code> <code>/articles/{article_id}</code> any fields (all optional) <code>null</code> <code>DELETE</code> <code>/articles/{article_id}</code> \u2014 deleted object"},{"location":"guide/getting-started/#schemas","title":"Schemas","text":"<p>The adapter decides which fields go into each schema:</p> <ul> <li>public (GET responses) \u2014 all model fields including <code>id</code></li> <li>create (POST) \u2014 all fields except <code>id</code> (generated by the database)</li> <li>update (PATCH) \u2014 same fields as create, but all optional</li> </ul>"},{"location":"guide/getting-started/#filtering","title":"Filtering","text":"<pre><code>GET /articles?published=true\nGET /articles?title=hello\n</code></pre>"},{"location":"guide/getting-started/#sorting","title":"Sorting","text":"<pre><code>GET /articles?sort=title\nGET /articles?sort=title:desc\nGET /articles?sort=title:asc&amp;sort=published:desc\n</code></pre>"},{"location":"guide/getting-started/#pagination","title":"Pagination","text":"<pre><code>GET /articles?page=2&amp;per_page=5\n</code></pre> <pre><code>{\n  \"page\": 2,\n  \"per_page\": 5,\n  \"count\": 42,\n  \"data\": [...]\n}\n</code></pre>"},{"location":"guide/getting-started/#next-steps","title":"Next steps","text":"<ul> <li>Schema Generation \u2014 control which fields appear in each schema</li> <li>Filtering &amp; Sorting \u2014 custom filters</li> <li>Nested Resources \u2014 nested routes</li> </ul>"},{"location":"guide/nested-resources/","title":"Nested Resources","text":"<p>Nested resources let you build hierarchical routes like <code>/threads/{thread_id}/posts/{post_id}</code>.</p>"},{"location":"guide/nested-resources/#basic-example","title":"Basic example","text":"<pre><code>from fastapi_crud_generator import CRUDCollection\nfrom fastapi_crud_generator.orm.sqlmodel import SQLModelAdapter\n\nthread_crud = CRUDCollection(orm_adapter=SQLModelAdapter(model=Thread, get_session=get_session))\npost_crud   = CRUDCollection(orm_adapter=SQLModelAdapter(model=Post,   get_session=get_session))\n\nthread_crud.add_nested_collection(\"/posts\", post_crud)\n\napp.include_router(thread_crud.get_router(prefix=\"/threads\"))\n</code></pre> <p>Resulting routes:</p> <pre><code>GET    /threads\nGET    /threads/{thread_id}\nPOST   /threads\nPATCH  /threads/{thread_id}\nDELETE /threads/{thread_id}\n\nGET    /threads/{thread_id}/posts\nGET    /threads/{thread_id}/posts/{post_id}\nPOST   /threads/{thread_id}/posts\nPATCH  /threads/{thread_id}/posts/{post_id}\nDELETE /threads/{thread_id}/posts/{post_id}\n</code></pre>"},{"location":"guide/nested-resources/#three-levels-deep","title":"Three levels deep","text":"<pre><code>category_crud = CRUDCollection(orm_adapter=SQLModelAdapter(model=Category, get_session=get_session))\nthread_crud   = CRUDCollection(orm_adapter=SQLModelAdapter(model=Thread,   get_session=get_session))\npost_crud     = CRUDCollection(orm_adapter=SQLModelAdapter(model=Post,     get_session=get_session))\n\nthread_crud.add_nested_collection(\"/posts\", post_crud)\ncategory_crud.add_nested_collection(\"/threads\", thread_crud)\n\napp.include_router(category_crud.get_router(prefix=\"/categories\"))\n# \u2192 /categories/{category_id}/threads/{thread_id}/posts/{post_id}\n</code></pre>"},{"location":"guide/nested-resources/#how-parent-filtering-works","title":"How parent filtering works","text":"<p>When a request comes in for <code>GET /threads/{thread_id}/posts</code>, the adapter receives <code>parent_refs</code> \u2014 a list of <code>ParentRef</code> objects containing the parent model and its PK:</p> <pre><code>@dataclass\nclass ParentRef:\n    model: type       # Thread\n    pk_values: BaseModel  # { thread_id: 42 }\n</code></pre> <p>The built-in adapters (SQLModel, SQLAlchemy, Tortoise) automatically apply <code>WHERE post.thread_id = 42</code> to queries and set <code>thread_id = 42</code> on creation.</p>"},{"location":"guide/nested-resources/#router-tags-for-nested-collections","title":"Router tags for nested collections","text":"<pre><code>from fastapi_crud_generator.config import NestedConfig\n\nthread_crud.add_nested_collection(\n    \"/posts\",\n    post_crud,\n    config=NestedConfig(router_kwargs={\"tags\": [\"posts\"]}),\n)\n</code></pre>"},{"location":"guide/orm-adapters/","title":"ORM Adapters","text":"<p>An adapter connects a specific ORM to <code>CRUDCollection</code>. It handles schema generation and executes database queries.</p>"},{"location":"guide/orm-adapters/#sqlmodel","title":"SQLModel","text":"<pre><code>from fastapi_crud_generator.orm.sqlmodel import SQLModelAdapter\n\nadapter = SQLModelAdapter(\n    model=Article,\n    get_session=get_session,  # async generator \u2192 AsyncSession\n)\n</code></pre> <p><code>get_session</code> is an async generator that yields a <code>sqlmodel.ext.asyncio.session.AsyncSession</code>. Standard FastAPI pattern:</p> <pre><code>from collections.abc import AsyncGenerator\nfrom sqlmodel.ext.asyncio.session import AsyncSession\n\nasync def get_session() -&gt; AsyncGenerator[AsyncSession, None]:\n    async with AsyncSession(engine) as session:\n        yield session\n</code></pre>"},{"location":"guide/orm-adapters/#sqlalchemy-declarativebase","title":"SQLAlchemy (DeclarativeBase)","text":"<p>Works directly with <code>DeclarativeBase</code> models, without SQLModel.</p> <pre><code>from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column\nfrom fastapi_crud_generator.orm.sqlalchemy import SQLAlchemyAdapter\n\nclass Base(DeclarativeBase):\n    pass\n\nclass Article(Base):\n    __tablename__ = \"article\"\n    id: Mapped[int] = mapped_column(primary_key=True)\n    title: Mapped[str]\n    content: Mapped[str]\n    published: Mapped[bool] = mapped_column(default=False)\n\nadapter = SQLAlchemyAdapter(\n    model=Article,\n    get_session=get_session,  # async generator \u2192 AsyncSession\n)\n</code></pre> <p><code>get_session</code> yields a <code>sqlalchemy.ext.asyncio.AsyncSession</code>.</p>"},{"location":"guide/orm-adapters/#tortoise-orm","title":"Tortoise ORM","text":"<p>Tortoise manages connections globally, so no <code>get_session</code> is needed.</p> <pre><code>from tortoise import fields\nfrom tortoise.models import Model\nfrom fastapi_crud_generator.orm.tortoise import TortoiseAdapter\n\nclass Article(Model):\n    id = fields.IntField(primary_key=True)\n    title = fields.CharField(max_length=200)\n    content = fields.TextField()\n    published = fields.BooleanField(default=False)\n\n    class Meta:\n        table = \"article\"\n\nadapter = TortoiseAdapter(model=Article)\n</code></pre> <p>Initialize Tortoise at application startup via lifespan:</p> <pre><code>from contextlib import asynccontextmanager\nfrom tortoise import Tortoise\n\n@asynccontextmanager\nasync def lifespan(app):\n    await Tortoise.init(\n        db_url=\"sqlite://:memory:\",\n        modules={\"models\": [\"myapp.models\"]},\n    )\n    await Tortoise.generate_schemas()\n    yield\n    await Tortoise.close_connections()\n\napp = FastAPI(lifespan=lifespan)\n</code></pre>"},{"location":"guide/orm-adapters/#comparison","title":"Comparison","text":"SQLModel SQLAlchemy Tortoise Requires <code>get_session</code> yes yes no Composite PK support yes yes no (Tortoise limitation) <code>crud_config</code> on model yes no no Schemas built from <code>model_fields</code> <code>mapped_column</code> <code>pydantic_model_creator</code>"},{"location":"guide/pagination/","title":"Pagination","text":""},{"location":"guide/pagination/#response-format","title":"Response format","text":"<p>Every <code>GET /</code> response is a <code>PaginatorPage</code> object:</p> <pre><code>{\n  \"page\": 1,\n  \"per_page\": 20,\n  \"count\": 142,\n  \"data\": [...]\n}\n</code></pre> <p><code>count</code> is the total number of matching objects, not the number of items in <code>data</code>.</p>"},{"location":"guide/pagination/#query-parameters","title":"Query parameters","text":"<pre><code>GET /articles?page=3&amp;per_page=10\n</code></pre> Parameter Default Constraint <code>page</code> 1 \u2265 1 <code>per_page</code> 20 \u2265 0 <p><code>per_page=0</code> returns all records without limit.</p>"},{"location":"guide/pagination/#custom-paginator","title":"Custom paginator","text":"<p>Subclass <code>PaginatorBase</code> to implement different pagination logic:</p> <pre><code>from fastapi import Query\nfrom fastapi_crud_generator.paginator import PaginatorBase\nfrom fastapi_crud_generator.schemas import PaginatorPage\n\nclass CursorPaginator(PaginatorBase):\n    def __init__(self, cursor: int = Query(0, ge=0), limit: int = Query(20, ge=1)):\n        self.cursor = cursor\n        self.limit = limit\n\n    async def paginate(self, session, query) -&gt; PaginatorPage:\n        data = (await session.exec(query.where(Model.id &gt; self.cursor).limit(self.limit))).all()\n        return {\"page\": 1, \"per_page\": self.limit, \"count\": len(data), \"data\": data}\n</code></pre> <p>Pass the custom paginator class through the adapter or via <code>dependency_overrides</code> in <code>CRUDCollection</code>.</p>"},{"location":"guide/schema-generation/","title":"Schema Generation","text":"<p>Request and response schemas are generated automatically from the ORM model. There is no need to write <code>ArticleCreate</code>, <code>ArticleUpdate</code>, or <code>ArticlePublic</code> by hand.</p>"},{"location":"guide/schema-generation/#what-gets-generated","title":"What gets generated","text":"<p>Given this model:</p> <pre><code>class Article(SQLModel, table=True):\n    id: int | None = Field(default=None, primary_key=True)\n    title: str\n    content: str\n    published: bool = False\n    created_at: datetime | None = Field(\n        default=None,\n        sa_column=Column(DateTime, server_default=func.now()),\n    )\n</code></pre> <p>The adapter automatically builds three schemas:</p> Schema Fields Used in public <code>id</code>, <code>title</code>, <code>content</code>, <code>published</code>, <code>created_at</code> GET responses create <code>title</code>, <code>content</code>, <code>published</code> POST body update <code>title?</code>, <code>content?</code>, <code>published?</code> PATCH body (all optional) <p><code>id</code> is excluded from create/update \u2014 it is generated by the database. <code>created_at</code> is also excluded \u2014 it has a <code>server_default</code>, so the database sets it automatically.</p>"},{"location":"guide/schema-generation/#controlling-the-field-sets","title":"Controlling the field sets","text":"<p>There are two ways to control which fields end up in the generated schemas, and you can use either:</p> <ul> <li>On the model, via a <code>crud_config</code> attribute \u2014 the default for every   collection built on that model (below).</li> <li>Per collection, via constructor params \u2014 overrides the model's config   for that one collection (see Passing fields at instantiation time).</li> </ul>"},{"location":"guide/schema-generation/#crud_config-on-the-model","title":"<code>crud_config</code> on the model","text":"<p>To use a different set of fields, add a <code>crud_config</code> attribute directly to the model. The adapter picks it up automatically.</p> <pre><code>from typing import ClassVar\nfrom fastapi_crud_generator.config import CRUDConfigDict\n\nclass Article(SQLModel, table=True):\n    id: int | None = Field(default=None, primary_key=True)\n    title: str\n    content: str\n    summary: str | None = None\n    internal_notes: str | None = None  # should not appear in the API\n\n    crud_config: ClassVar[CRUDConfigDict] = CRUDConfigDict(\n        public_fields={\"id\", \"title\", \"summary\"},\n        create_fields={\"title\", \"content\", \"summary\"},\n        update_fields={\"title\", \"content\", \"summary\"},\n    )\n</code></pre> <p>Every built-in adapter reads <code>crud_config</code> off the model, so this works the same on SQLModel, SQLAlchemy and Tortoise. Only how you attach it differs:</p> <ul> <li>SQLModel \u2014 annotate it <code>ClassVar</code>, otherwise the model treats it as a   table column (as shown above).</li> <li>SQLAlchemy / Tortoise \u2014 a plain class attribute is enough; neither   turns an unmapped attribute into a column.</li> </ul> <pre><code># SQLAlchemy (DeclarativeBase)\nclass Article(Base):\n    __tablename__ = \"article\"\n    # ... mapped columns ...\n    crud_config = CRUDConfigDict(create_fields={\"title\", \"content\"})\n\n# Tortoise\nclass Article(Model):\n    # ... fields ...\n    crud_config = CRUDConfigDict(create_fields={\"title\", \"content\"})\n</code></pre>"},{"location":"guide/schema-generation/#base_fields-shared-fields-across-schemas","title":"<code>base_fields</code> \u2014 shared fields across schemas","text":"<p>If several schemas share a common set of fields, put them in <code>base_fields</code>:</p> <pre><code>crud_config: ClassVar[CRUDConfigDict] = CRUDConfigDict(\n    base_fields={\"title\", \"content\"},\n    public_fields={\"id\", \"published\"},    # \u2192 id, published, title, content\n    create_fields={\"category_id\"},        # \u2192 category_id, title, content\n)\n</code></pre> <p><code>base_fields</code> is merged into each explicitly defined set.</p>"},{"location":"guide/schema-generation/#using-a-hand-written-schema","title":"Using a hand-written schema","text":"<p>Any schema can be replaced with a custom Pydantic model. The rest continue to be generated automatically.</p> <pre><code>class ArticleCreate(BaseModel):\n    title: str\n    content: str\n    author_id: int  # a field that is not on the ORM model\n\ncrud = CRUDCollection(\n    orm_adapter=adapter,\n    create_schema=ArticleCreate,  # only this schema is custom\n)\n</code></pre>"},{"location":"guide/schema-generation/#passing-fields-at-instantiation-time","title":"Passing fields at instantiation time","text":"<p>Field sets can also be passed directly to <code>CRUDCollection</code>, without touching the model:</p> <pre><code>crud = CRUDCollection(\n    orm_adapter=adapter,\n    public_fields={\"id\", \"title\"},\n    create_fields={\"title\", \"content\"},\n)\n</code></pre> <p>This is useful when the same model is used in multiple <code>CRUDCollection</code> instances with different field sets.</p>"}]}